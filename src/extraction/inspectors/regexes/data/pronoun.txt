(?:
    (?:\{\{)
    |
    (?:==)
)\s*

(?:
    (?:
        forma\s+(?:de\s+)?pronombre(?:\s+(?<kind_1>\p{L}{4,}))?
    )

    |

    (?:
        pronombre\s*

        (?:
            (?<kind_2>\p{L}{4,})
            |
            (?:\|\s*[^|}]+(\|\s*(?<kind_3>\p{L}{4,}))?)
            |
            [=\}]
        )
    )
)